services:
  - type: web
    name: helixmed-ai-backend
    runtime: python
    rootDir: apps/training
    buildCommand: pip install -r requirements.txt
    startCommand: python inference_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 10000
